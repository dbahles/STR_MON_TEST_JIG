#include "calibration.h"

namespace Calibration
{
    void begin()
    {
        // Future: load calibration values from non-volatile storage.
    }

    bool isValid()
    {
        return true;
    }
}
